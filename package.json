{
  "name": "feedback-monorepo",
  "private": true,
  "version": "0.4.0",
  "description": "跨项目个人反馈组件与归档服务",
  "type": "module",
  "engines": {
    "node": ">=22.13"
  },
  "packageManager": "pnpm@11.23.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "dev": "pnpm --filter @feedback/server dev"
  }
}
